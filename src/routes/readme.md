# 📌 API Routes Documentation (Restaurant ERP)

## 1️⃣ Auth (المصادقة)

| Method | Endpoint         | Description                |
| ------ | ---------------- | -------------------------- |
| POST   | `/auth/register` | تسجيل مستخدم جديد          |
| POST   | `/auth/login`    | تسجيل الدخول               |
| POST   | `/auth/logout`   | تسجيل الخروج               |
| GET    | `/auth/me`       | عرض بيانات المستخدم الحالي |

---

## 2️⃣ Staff (الموظفين)

| Method | Endpoint     | Description       |
| ------ | ------------ | ----------------- |
| GET    | `/staff`     | عرض جميع الموظفين |
| GET    | `/staff/:id` | عرض موظف محدد     |
| POST   | `/staff`     | إضافة موظف جديد   |
| PUT    | `/staff/:id` | تعديل بيانات موظف |
| DELETE | `/staff/:id` | حذف موظف          |

---

## 3️⃣ Roles & Permissions (الأدوار والصلاحيات)

| Method | Endpoint       | Description      |
| ------ | -------------- | ---------------- |
| GET    | `/roles`       | عرض جميع الأدوار |
| POST   | `/roles`       | إنشاء دور جديد   |
| PUT    | `/roles/:id`   | تعديل دور        |
| DELETE | `/roles/:id`   | حذف دور          |
| GET    | `/permissions` | عرض كل الصلاحيات |

---

## 4️⃣ Tables (الطاولات)

| Method | Endpoint      | Description        |
| ------ | ------------- | ------------------ |
| GET    | `/tables`     | عرض جميع الطاولات  |
| GET    | `/tables/:id` | عرض طاولة معينة    |
| POST   | `/tables`     | إنشاء طاولة جديدة  |
| PUT    | `/tables/:id` | تعديل بيانات طاولة |
| DELETE | `/tables/:id` | حذف طاولة          |

---

## 5️⃣ Reservations (الحجوزات)

| Method | Endpoint            | Description       |
| ------ | ------------------- | ----------------- |
| GET    | `/reservations`     | عرض جميع الحجوزات |
| GET    | `/reservations/:id` | عرض حجز معين      |
| POST   | `/reservations`     | إنشاء حجز جديد    |
| PUT    | `/reservations/:id` | تعديل بيانات حجز  |
| DELETE | `/reservations/:id` | إلغاء حجز         |

---

## 6️⃣ Menu & Categories (المنيو والتصنيفات)

| Method | Endpoint          | Description        |
| ------ | ----------------- | ------------------ |
| GET    | `/categories`     | عرض جميع التصنيفات |
| POST   | `/categories`     | إنشاء تصنيف جديد   |
| PUT    | `/categories/:id` | تعديل تصنيف        |
| DELETE | `/categories/:id` | حذف تصنيف          |
| GET    | `/menu`           | عرض جميع الأطباق   |
| GET    | `/menu/:id`       | عرض طبق معين       |
| POST   | `/menu`           | إضافة طبق جديد     |
| PUT    | `/menu/:id`       | تعديل بيانات طبق   |
| DELETE | `/menu/:id`       | حذف طبق            |

---

## 7️⃣ Orders (الطلبات)

| Method | Endpoint      | Description         |
| ------ | ------------- | ------------------- |
| GET    | `/orders`     | عرض جميع الطلبات    |
| GET    | `/orders/:id` | عرض تفاصيل طلب معين |
| POST   | `/orders`     | إنشاء طلب جديد      |
| PUT    | `/orders/:id` | تعديل بيانات طلب    |
| DELETE | `/orders/:id` | حذف طلب             |

---

## 8️⃣ Invoices (الفواتير)

| Method | Endpoint        | Description        |
| ------ | --------------- | ------------------ |
| GET    | `/invoices`     | عرض جميع الفواتير  |
| GET    | `/invoices/:id` | عرض فاتورة معينة   |
| POST   | `/invoices`     | إنشاء فاتورة جديدة |
| PUT    | `/invoices/:id` | تعديل فاتورة       |
| DELETE | `/invoices/:id` | حذف فاتورة         |

---

## 9️⃣ Inventory (المخزون)

### Suppliers (الموردين)

| Method | Endpoint         | Description       |
| ------ | ---------------- | ----------------- |
| GET    | `/suppliers`     | عرض جميع الموردين |
| POST   | `/suppliers`     | إضافة مورد جديد   |
| PUT    | `/suppliers/:id` | تعديل بيانات مورد |
| DELETE | `/suppliers/:id` | حذف مورد          |

### Products / Ingredients (المنتجات والمكونات)

| Method | Endpoint           | Description       |
| ------ | ------------------ | ----------------- |
| GET    | `/ingredients`     | عرض جميع المنتجات |
| GET    | `/ingredients/:id` | عرض منتج معين     |
| POST   | `/ingredients`     | إضافة منتج جديد   |
| PUT    | `/ingredients/:id` | تعديل منتج        |
| DELETE | `/ingredients/:id` | حذف منتج          |

---

## 🔟 Reports (التقارير)

| Method | Endpoint                | Description    |
| ------ | ----------------------- | -------------- |
| GET    | `/reports/sales`        | تقرير المبيعات |
| GET    | `/reports/inventory`    | تقرير المخزون  |
| GET    | `/reports/reservations` | تقرير الحجوزات |
| GET    | `/reports/orders`       | تقرير الطلبات  |

---
